# 任务卡 UX 修复总结（2026-08-10）

## 问题现象（用户实测截图）
窗口模式聊天中，触发「帮我做一个番茄钟」任务后出现时序倒挂：
- 任务卡（已完成 56.2s · 6 次工具调用）渲染在 AI 确认播报「好嘞主人，番茄钟就交给我啦！」**上方**；
- AI 完成汇报「主人，任务都办妥啦…」排在更下方。
- 视觉上造成「任务已经跑完，AI 才刚答应」的错乱感。

## 根因
`WindowModeView.tsx` 的锚点策略（v2/v3）把任务卡固定插入在「触发任务的用户消息」正下方。
而 AI 确认播报是 `run_start` 之后才由 shell 转述、追加到消息流末尾的，于是任务卡被静态夹在用户消息与 AI 确认回复之间，时序错位。

## 修复
### 1. v4 锚点迁移（`frontend/src/components/WindowModeView.tsx`）
新增 effect：锚点仍指向 `user` 消息、且该消息之后已出现第一条 `role==='ai'` 消息时，把锚点迁移到这条 AI 消息。
- 之后锚点是 AI 消息不再移动 → 完成汇报自然排在卡片之后；
- 与 v3 重锚协同：新 run 先落回用户消息 → v4 再迁移到 AI 确认消息，顺序不冲突。
- 预期时序：**用户请求 → AI 确认 → 任务执行卡 → AI 完成汇报**。

### 2. 左对齐（`frontend/src/styles/global.css`）
`.chat-bubble-row.task` 增加 `padding-left: 38px`（头像 30px + gap 8px），使任务卡与 AI 气泡左边缘同起点。

## 校验
- `npx tsc --noEmit`：0 错误
- 前端 5173 / 后端 12393 均返回 200
- 锚点逻辑审查：v2（兜底/失效重置）+ v3（run_start 重锚）+ v4（AI 消息迁移）三段协同，无竞态

## 需在界面复测
发一条任务消息（如「帮我做一个番茄钟」），确认：
1. 任务卡出现在 AI 确认「好嘞主人」**下方**、完成汇报**上方**；
2. 任务卡左侧与 AI 气泡左边缘对齐。
若仍偏差，请截图反馈，按实际渲染顺序精准定位。

---

# 追加：产物点击 + 新任务识别修复（2026-08-10 16:45）

## Bug 1：点击生成文件 `pomodoro.html` 报「path 不在任务工作目录内」
### 根因
前端产物列表里的路径是**相对路径**（LLM 以 cwd=workspace 写盘，如 `pomodoro.html`）。
后端 `open_task_dir_route` 用 `Path(raw).resolve()` 解析——相对路径按**后端进程 cwd** 解析，
与 `task.workspace` 比对必然越界 → 400「path 不在任务工作目录内」。

### 修复（双保险）
- 后端 `task_route.py`：相对路径先基于 `ws` 拼接再 `resolve()`；错误文案改为「文件不在任务工作目录内，请改用「打开工作目录」查看」。
- 前端 `TaskStreamPanel.tsx`：点击产物时，非绝对路径先补全为 `workspace/xxx` 再传给后端。

### 验证（真实接口）
`POST /api/tasks/{id}/open {"path":"pomodoro.html"}` → `{"ok":true,"revealed":"...\task-20260810163213\pomodoro.html"}`（修复前 400）。
绝对路径 counter.html → 200，回归不破。

## Bug 2：任务完成后发「再做一个计数器」复用旧任务「番茄钟」
### 根因
`useTaskMode.send`：同一会话、任务已完成（非运行中）时一律 `startRun(activeTask.id, text)` 续跑旧任务
→ 新请求被塞进旧任务，标题/goal/上下文全错（LLM 仍停在旧任务语境）。

### 修复
`useTaskMode.ts` 新增 `NEW_TASK_REQUEST_RE` 句式判定：
开头为「再/重新/新/另/麻烦再/请再（可选 帮我/给我）」或「帮我/给我/麻烦帮我/请帮我（可选 再）」
+ 做一个/做个/弄一个/弄个/来一个/整个/生成一个 → **创建新任务**（createAndRun）。
不匹配（如「把按钮改成红色」「加个暂停功能」）→ 仍续跑旧任务。
正则 10 用例全过（含「再帮我做个表格」「帮我再做个表格」）。

## 校验
- 前端 `npx tsc --noEmit`：0 错误
- 后端 `py_compile`：通过
- 后端重启（新 PID 26404），12393 返回 200
- 产物点击相对路径/绝对路径均验证通过
